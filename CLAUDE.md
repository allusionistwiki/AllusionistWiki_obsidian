# CLAUDE.md — 幻想再帰のアリュージョニスト Wiki 設計書

本ファイルは「幻想再帰のアリュージョニスト」（以下、本作）の分析Wikiの**構成(schema)・命名規約・ワークフロー・鉄則**を定義する。Obsidian vaultとして`wiki/`を開くことを前提とする。

> 設計思想の出典：Karpathy Gist（本を読む→factsページ化＋query応答）、zenn実運用記事（「価値は要約ではなく繋げる力」「推測や原文にない情報を足さない」）。詳細は [`./DESIGN_NOTES.md`](./DESIGN_NOTES.md) 。

## 0. プロジェクトの目的
- 既存コミュニティwiki（wikiwiki.jp/gensousaiki）が苦手とする**「多層アナロジー（引喩・神話参照・展開の相似/相違）」**を典拠付きで体系化し、上回る。
- **1話ご感想**を解釈層として蓄積する。
- 純粋な事実ページは「原文典拠付き」を保ち、感想/解釈と厳格に分離する（幻覚汚染防止）。

## 1. ディレクトリ構造
```
幻想再帰のアリュージョニスト-wiki/
├── CLAUDE.md            # この設計書（本ファイル）
├── DESIGN_NOTES.md      # 設計判断の記録・議論履歴
├── raw/                 # 原文抽出（不変・読み取り専用・追記のみ）
│   └── <話数>_<タイトル>_[分割ID].md
├── assets/              # 画像などローカル保存
└── wiki/                # Obsidian vault（ここに書く）
    ├── index.md         # トップカタログ（最新話一覧＋リンク）
    ├── log.md           # 操作履歴（追記のみ）
    ├── CLAUDE.md        # （任意）vault固有の細ルール
    ├── episodes/        # ★1話ごと：要約+人物+引喩+感想+疑問
    │   └── chNNN.md
    ├── characters/      # 事実ページ（初出/名称/関係/典拠）
    │   └── <人物>.md
    ├── world/           # 世界観・用語・力体系（概念ページ）
    │   └── <設定>.md
    ├── allusions/       # ★核心層：神話/文学/思想リソース別
    │   └── <リソース>.md
    ├── analogies/       # ★核心層：展開の相似/相違（再帰構造）
    │   └── recursion-map.md
    └── reflections/     # ★感想専用層（事実から分離）
        ├── by-episode/  # chNNN.md
        └── by-theme/    # テーマ別
```

## 2. 命名規約（必須）
| 種別 | 名前 | 例 |
|---|---|---|
| raw | `話数_タイトル_[分割ID].md`（分割は必要なら） | `001_棒（ワンド）_01.md` |
| episode | `chNNN.md`（NNN=通し話数、ゼロパディング3桁） | `ch001.md` |
| character/world/allusion | 見出し名そのまま（スラッシュ可） | `トリシューラ.md`, `【杖】と呪術体系.md` |
| reflection | `by-episode/chNNN.md` / `by-theme/<テーマ>.md` | — |

- **rawの分割ID**：1話が長大（本作「棒（ワンド）」は約200ページ）な場合は `_01`,`_02`…で分割。分割単位は概ね25〜30ページまたはシーン区切り。
- PDF頁番号と印刷頁番号は **1ずれる**（PDF leaf N = 本文印刷 N-1）。rawのヘッダは `【p{leaf}】` 形式で統一（既存ファイル準拠）。

## 3. ページ種別ごとの必須フィールド
### episodes/chNNN.md
1. ヘッダ：話数・タイトル・PDF頁範囲・源rawファイル一覧
2. **要約**：時系列順に事実を述べる（解釈を挿入しない）
3. **登場人物**：`[[人名]]` リンク付きで初出役割を記す
4. **引喩・アナロジー**：各引喩を「原典／本作での使われ方／典拠頁」で記す
5. **感想セクション**：本話の読後感（→ `reflections/by-episode/chNNN.md`へ詳細）
6. **疑問・解析ノート**：読みながら浮かんだ疑問・未確定事項・用語整理・組織/人物の対比・一人多名称

### characters/<人物>.md
- 名称バリエート一覧（別名・呼称）、初出、外見・能力、所属/関係（`[[ ]]`）、目的、**典拠箇所**（pp./raw）
- 「推測される内面」は事実ではなく `> [!] 解釈：...` blockquoteで分離

### world/<設定>.md
- 定義、作中ルール、他概念との境界、典拠。未確定は「要検証」ラベル

### allusions/<リソース>.md（核心層）
- **原典の説明**（正確に・出典付き＝ここが既存wikiを上回る部分）
- **本作での使われ方**（どの話・どの場で・何を意図したと読みうるか）
- **[[関連エピソード]]** ワイルink
- 確度ラベル：`【確定】【相当有力】【推測・要検証】`

### analogies/recursion-map.md
- 「どの話がどの話を反復/転倒/否定するか」を辺で表現。作品題名の「再帰」構造を可視化。

### reflections/by-episode/chNNN.md（感想専用層）
- 主観的読後感・美学判断・仮説。**事実と混同しないよう冒頭に「解釈層であること」を明記**。

## 4. ワークフロー（標準流れ）
1. **ingest**：`pypdf.PdfReader().extract_text()` で原文抽出（ToUnicode適用済みで文字化けしない）。raw/に新規則で保存（不変）。
2. **区切り検出**：太字タイトル。本作は `幕間　『…』` マーカーと `第X章 / N－NN タイトル` が目印。抽出テキストの単独行を正規表現検出（`幕間|第\d+|N－\d+`）。
3. **episodeページ生成**：要約→人物→引喩→感想→疑問ノート。
4. **事実ページ生成**：登場人物・設定を characters/world へ（典拠付き）。
5. **アナロジー解析**：allusions（原典）＋analogies（再帰マップ）。**LLMの世界知識+推論が必須**（頻度抽出だけでは「オジデウス神話のパロディ」は判別不可）。
6. **感想**：reflections/by-episode へ。
7. **lint**（§5）を回し、切れリンク・典拠欠落を修正。

## 5. Lintチェックリスト（生成後必ず確認）
- [ ] 全 `[[ ]]` が実ファイルに対応している（切れリンク無し）
- [ ] 事実行に典拠（pp./raw）が付いている
- [ ] 感想/解釈が facts から分離されている（混入していない）
- [ ] 推測情報はラベル付き・原文にない事実ではない
- [ ] raw が不変である（既存本文を改っていない）

## 6. 鉄則（zenn準拠）
- **推測や原文にない情報を足さない**。尤其「引喩の存在」自体を確定させるには原典知識が必要だが、**原典の説明は正確に、本作への当てはめは解釈として明示**。
- 事実ページと感想層は**物理的に別ディレクトリ**（混染防止）。

## 7. 本作の特記（分析の前提知識）
- 「ネットミームから現代思想まで引喩が散りばめたオカルトパンク」。命は**多層アナロジー**。
- 話区切り：`幕間　『…』`（例 p1028『棒（ワンド）』, p1231『もし狂犬が戦国乱世に転生したら』, p1282『もうひとつの左手』, p1308『もうひとつの悪夢』）。超大編「棒（ワンド）」は約200ページ。
- 話タイトルは**太字**。PDF本文に印刷頁番号を内含（`【p{leaf}】` ヘッダで復元）。
